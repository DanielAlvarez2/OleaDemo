<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manager</title>
    
    <link rel="stylesheet" href="manager.css">
    <script
      src="https://kit.fontawesome.com/21ef82ed30.js"
      crossorigin="anonymous"
    ></script>
    <style>
      
      .special{
            margin-bottom: 0px;
        }
       
        #controls{
            line-height:30px;
            font-size:19px;
            padding-left:5px;
        }
        #riojas, #controls{
            width:5in;
            background:white;
            border:solid 1px black;
            display:inline-block;
            
        }
        .liquor{
            font-weight: 900;
            text-align: center;
        }
        #riojas{
            padding: 0px 0px;
            height:8in;
            position: relative;
        }
        #riojas{
            line-height: 
            <%
                for (let i=0; i<info.length; i++){
                    if (info[i].category == 'controls-liquor' && info[i].name == 'lineHeight'){ %>
                        <%= info[i].pixels %>
            <%
                    }
                }
            %>; 
            padding-left:
            <% 
                for (let i=0; i<info.length; i++){
                    if (info[i].category == 'controls-liquor' && info[i].name == 'padding-sides'){ %>
                        <%= info[i].pixels%>
            <%
                    } 
                } 
            %>;
            
            padding-right:
            <% 
                for (let i=0; i<info.length; i++){
                    if (info[i].category == 'controls-liquor' && info[i].name == 'padding-sides'){ %>
                        <%= info[i].pixels%>
            <%
                    } 
                } 
            %>;
            
        }
        
        ul{
            font-family:serif;
        }
        
        #riojas{
            font-size:
            <% 
                for (let i=0; i<info.length; i++){
                    if (info[i].category == 'controls-liquor' && info[i].name == 'liquorFontSize'){ %>
                        <%= info[i].pixels%>
            <%
                    } 
                } 
            %>;

        }
        #riojas li{
            padding-top:            
            <% 
                for (let i=0; i<info.length; i++){
                    if (info[i].category == 'controls-liquor' && info[i].name == 'padding-between'){ 
                        let half=(info[i].pixels).substring(0,info[i].pixels.length-2)/2
                        %>
                        <%= half%>px
            <%
                    } 
                } 
            %>;
            padding-bottom:
            <% 
                for (let i=0; i<info.length; i++){
                    if (info[i].category == 'controls-liquor' && info[i].name == 'padding-between'){ 
                        let half=(info[i].pixels).substring(0,info[i].pixels.length-2)/2
                        %>
                        <%= half%>px
            <%
                    } 
                } 
            %>;
        }
        input{
            text-align: center;
        }
        #flexbox{display:flex;}
        @media (orientation: landscape){
            #flexbox{
                flex-direction: row;
            }
          
        }
        @media (orientation: portrait){
            #flexbox{
                flex-direction: column;
            }
           

        }
        li{
            max-width: none;
        }
    </style>
</head>










<body>

    <div id="wrapper">
    <div id="content">

    <nav>
        <h1>olea<span id="content-manager">Content Manager</span></h1>
        <ul>
            <li class="active-page">Specials</li>
            <a href="/dinnerUpdateCuredMeats"><li>Dinner</li></a>
            <li>Dessert</li>
            <li>Wine/Drinks</li>
            <a href="/archive"><li id="archive">Archive</li></a>
        </ul>
        <ul id="nav-ul-bottom">
            <a href="/manager"><li id="specials-edit">Edit Menu Items</li></a>
            <li class="active-page" id="specials-format-layout">Format Layout</li>
            <a href="/liquorPrint"><li id="liquor-print">Print Preview</li></a>
            </ul>
    </nav>
    
    
    
    <h1 id="page-title">liquor</h1>
    <div id="flexbox">    

            <div id="controls">
                <form method="POST" action="/saveChangesLiquor">
                    FORMAT PAGE LAYOUT<br>
                    Page Space Left & Right: <input name="liquorPaddingSides" id="liquor-padding-sides" type="text" size="2"> px min10px<br>
                    Space between menu items: <input name="liquorPaddingBetween" id="liquor-padding-between" type="text" size="2"> px <br>
                    Font Size: <input name="liquorFontSize" id="liquor-font-size" type="text" size="2"> px<br>
                    Line Height: <input name="lineHeight" id="line-height" type="text" size="2"> px <br>
                    <button id="clear-changes">Clear Changes</button> 
                    <input type="submit" id="save-changes" value="Save Changes"> 
                    <button id="liquor-print-preview-button">Print Preview</button>
                </form>
            </div><!--#controls-->















                
            <div id="riojas" style="display:flex;padding-top:10px;">
            <div id="left">
                <div class="liquor">VODKA</div>
                <ul>
                <% for(let i=0; i<info.length;i++){ %>
                    <% if (info[i].type == "Vodka"){ %>
                    <li class="special">

                        <span><%= info[i].name %></span> / 
                        <span>$<%= info[i].price %></span>
                    </li>
                    <% } %>
                <% } %>
                </ul>

                <br>

                <div class="liquor">GIN</div>
                <ul>
                <% for(let i=0; i<info.length;i++){ %>
                    <% if (info[i].type == "Gin"){ %>
                    <li class="special">

                        <span><%= info[i].name %></span> / 
                        <span>$<%= info[i].price %></span>
                    </li>
                    <% } %>
                <% } %>
                </ul>

                <br>

                <div class="liquor">RUM</div>
                <ul>
                <% for(let i=0; i<info.length;i++){ %>
                    <% if (info[i].type == "Gin"){ %>
                    <li class="special">

                        <span><%= info[i].name %></span> / 
                        <span>$<%= info[i].price %></span>
                    </li>
                    <% } %>
                <% } %>
                </ul>

                <br>

                <div class="liquor">TEQUILA</div>

                <ul>
                <% for(let i=0; i<info.length;i++){ %>
                    <% if (info[i].type == "Tequila Silver"){ %>
                    <li class="special">

                        <span><%= info[i].name %></span> / 
                        <span>$<%= info[i].price %></span>
                    </li>
                    <% } %>
                <% } %>
                </ul>
                <br>
                <ul>
                <% for(let i=0; i<info.length;i++){ %>
                    <% if (info[i].type == "Tequila Reposado"){ %>
                    <li class="special">

                        <span><%= info[i].name %></span> / 
                        <span>$<%= info[i].price %></span>
                    </li>
                    <% } %>
                <% } %>
                </ul>
                <br>
                <ul>
                <% for(let i=0; i<info.length;i++){ %>
                    <% if (info[i].type == "Tequila Añejo"){ %>
                    <li class="special">

                        <span><%= info[i].name %></span> / 
                        <span>$<%= info[i].price %></span>
                    </li>
                    <% } %>
                <% } %>
                </ul>

                </div><!--#left-->
                <div id="right">
                   <div class="liquor">BOURBON</div>
                        <ul>
                        <% for(let i=0; i<info.length;i++){ %>
                            <% if (info[i].type == "Bourbon"){ %>
                            <li class="special">

                                <span><%= info[i].name %></span> / 
                                <span>$<%= info[i].price %></span>
                            </li>
                            <% } %>
                        <% } %>
                        </ul>

                        <br>

                        <div class="liquor">RYE</div>
                        <ul>
                        <% for(let i=0; i<info.length;i++){ %>
                            <% if (info[i].type == "Rye"){ %>
                            <li class="special">

                                <span><%= info[i].name %></span> / 
                                <span>$<%= info[i].price %></span>
                            </li>
                            <% } %>
                        <% } %>
                        </ul>

                        <br>

                        <div class="liquor">SCOTCH</div>
                        <ul>
                        <% for(let i=0; i<info.length;i++){ %>
                            <% if (info[i].type == "Scotch"){ %>
                            <li class="special">

                                <span><%= info[i].name %></span> / 
                                <span>$<%= info[i].price %></span>
                            </li>
                            <% } %>
                        <% } %>
                        </ul>

                        <br>

                        <div class="liquor">JAPANESE WHISKY</div>
                        <ul>
                        <% for(let i=0; i<info.length;i++){ %>
                            <% if (info[i].type == "Japanese Whisky"){ %>
                            <li class="special">

                                <span><%= info[i].name %></span> / 
                                <span>$<%= info[i].price %></span>
                            </li>
                            <% } %>
                        <% } %>
                        </ul>
                        
                </div><!--#right-->
                    
                    
                    <div id="printer-dead-space" class="no-print"></div>
            </div><!--#riojas-->
            
            
            </div><!--#flexbox-->

            <footer>
                <ul>
                    <li>Specials</li>
                    <li>Dinner</li>
                    <li>Dessert</li>
                    <li>Wine/Drinks</li>
                    <li>Archive</li>
                </ul>
            </footer>
            </div><!--#content-->
            </div><!--#wrapper-->
            
            <script>
                let riojasPage = document.querySelector('#riojas')
                let liquorPaddingSides = document.querySelector('#liquor-padding-sides')
                let liquorPaddingBetween = document.querySelector('#liquor-padding-between')
                let liquorFontSize = document.querySelector('#liquor-font-size')
                let p = document.querySelectorAll('.special')
                let clearChanges = document.querySelector('#clear-changes')
                let liquorPrintPreviewButton = document.querySelector('#liquor-print-preview-button')
                let special = document.querySelectorAll('.special')
                let lineHeight = document.querySelector('#line-height')

                lineHeight.addEventListener('input', function(evt){
                    riojasPage.style.lineHeight = `${Number(this.value)}px`
                })

                liquorPrintPreviewButton.addEventListener('click',(event)=>{
                    event.preventDefault()
                    location.replace('/riojasPrintPreview')
                })
                clearChanges.addEventListener('click',(event)=>{
                    event.preventDefault()
                    location.reload()
                })
                liquorPaddingSides.addEventListener('input', function(evt){
                    riojasPage.style.paddingLeft = `${Number(this.value)}px`
                    riojasPage.style.paddingRight = `${Number(this.value)}px`
                })
                liquorPaddingBetween.addEventListener('input', function(evt){
                    p.forEach((special,i)=>{
                        special.style.paddingTop = `${(Number(this.value))/2}px`
                        special.style.paddingBottom = `${(Number(this.value))/2}px`
                    })
                })
                liquorFontSize.addEventListener('input', function(evt){
                    
                        riojasPage.style.fontSize = `${Number(this.value)}px`
                })
            
            </script>
        <script src="main.js"></script>
       
</body>
</html>